import { StyleSheet } from "@react-pdf/renderer"

export default StyleSheet.create({
    page: {
        display: "flex",
        flexDirection: "column"
    },
    body: {
        marginTop: "48.25mm"
    },
    heading: {
        alignItems: "center",
        fontSize: 12,
        fontWeight: "bold",
        // fontStyle: 
    }
})